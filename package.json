{
  "name": "noetic",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mattapperson/noetic.git"
  },
  "bugs": {
    "url": "https://github.com/mattapperson/noetic/issues"
  },
  "homepage": "https://github.com/mattapperson/noetic#readme",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@noetic-tools/acp": "workspace:*",
    "@noetic-tools/core": "workspace:*",
    "@noetic-tools/openui": "workspace:*",
    "bun-types": "latest",
    "yaml": "^2.9.0",
    "zod": "^4.0.0"
  },
  "scripts": {
    "postinstall": "node scripts/patch-sdk.js",
    "test": "bun --cwd=packages/types run test && bun --cwd=packages/context run test && bun --cwd=packages/openui run test && bun --cwd=packages/agent-plugins run test && bun --cwd=packages/acp run test && bun --cwd=packages/chat-sdk run test && bun --cwd=packages/core run test && bun --cwd=packages/eval run test && bun --cwd=packages/inspector run test",
    "test:coverage": "bun --cwd=packages/types run test:coverage && bun --cwd=packages/context run test:coverage && bun --cwd=packages/openui run test:coverage && bun --cwd=packages/agent-plugins run test:coverage && bun --cwd=packages/acp run test:coverage && bun --cwd=packages/chat-sdk run test:coverage && bun --cwd=packages/core run test:coverage && bun --cwd=packages/eval run test:coverage && bun --cwd=packages/inspector run test:coverage",
    "test:ci": "bun --cwd=packages/types run test:ci && bun --cwd=packages/context run test:ci && bun --cwd=packages/openui run test:ci && bun --cwd=packages/agent-plugins run test:ci && bun --cwd=packages/acp run test:ci && bun --cwd=packages/chat-sdk run test:ci && bun --cwd=packages/core run test:ci && bun --cwd=packages/eval run test:ci && bun --cwd=packages/inspector run test:ci",
    "lint": "bunx biome check .",
    "lint:fix": "bunx biome check --fix .",
    "format": "bunx biome format --write .",
    "format:check": "bunx biome format .",
    "check:exports": "bun scripts/check-export-tags.ts",
    "typecheck:examples": "bunx tsc --noEmit -p examples",
    "example:acp": "bun examples/acp-e2e.ts",
    "inspect": "bun packages/inspector/server/host.ts"
  },
  "dependencies": {
    "@openrouter/agent": "^0.6.0",
    "@openrouter/sdk": "^0.12.12"
  }
}
