/**
 * Ambient preamble injected into bare documentation code fences by
 * `scripts/check-docs.ts`. It imports the full public surface of
 * `@noetic-tools/core` plus a handful of stand-in symbols the docs reference
 * narratively, so that snippets which omit imports still type-check.
 *
 * GENERATED from `packages/core/src/index.ts`. Regenerate with:
 *   bun scripts/gen-doc-preamble.ts
 * Do not edit the import lists by hand.
 */

export const DOC_PREAMBLE =
  "// Injected by check-docs.ts for bare snippets. Not part of the doc source.\nimport type {\n  AfterFirstTurnContext, AfterModelCallParams, AfterModelCallResult, AgentConfig, AgentHarnessContract, AgentHooks,\n  AskUserAnnotation, AskUserInput, AskUserOption, AskUserOutput, AskUserQuestion, BeforeToolCallParams,\n  BeforeToolCallResult, BranchRoute, BranchWorkflowNode, BudgetConfig, CallModelRequest, Channel,\n  ChannelHandle, CheckpointSnapshot, CheckpointStore, CompleteParams, Condition, ContentPart,\n  Context, ContextConfig, ContextData, ContextHarness, ContextLayer, ContextLayerHooks,\n  ContextScope, ConvergeConfig, CreateCheckpointStoreOptions, CreateNudgeMessageOpts, CwdSnapshot, CwdState,\n  DeliveryMode, DetachedHandle, DetachedSignal, DeveloperMessageExtensionItem, DisposeParams, DurableTaskState,\n  DynamicWorkflowOpts, EmbedFn, EveryOptions, EveryWorkflowNode, ExecuteInput, ExecuteOptions,\n  ExecuteStepFn, ExecutionContext, ExecutionOutcome, ExtendedItem, ExtensionItem, ExternalChannel,\n  FactExtractor, FactSearcher, FileSearchItem, ForkWorkflowNode, FrameworkStreamEvent, FrontierFrame,\n  FsAdapter, FsStats, FunctionCallItem, FunctionCallOutputItem, HarnessResponse, HarnessStatus,\n  HistoryWindowConfig, HydrationContext, ImageGenerationItem, InferContext, InferContextShape, InferExtendedItem,\n  InitParams, InitResult, InputContentPart, InputFilePart, InputImagePart, InputMessageItem,\n  InputTextPart, InterviewOpts, InterviewQuestionAnswer, InterviewResult, Item, ItemBase,\n  ItemLog, ItemLogSnapshot, ItemSchemaExtensions, LLMResponse, LastLayerUsage, LayerCallModelRequest,\n  LayerDataDecl, LayerFunctionDecl, LayerProvides, LayerTimeouts, LayerTraceSpan, LayerUsageEntry,\n  LedgerEntry, LlmProviderConfig, LlmWorkflowNode, LoopConfig, LoopWorkflowNode, MergeStrategy,\n  MessageItem, ModelParams, NoeticError, ObservationalContextConfig, ObservationalState, OtherwiseClause,\n  OutputItem, OutputTextPart, ParseAndRunWorkflowOpts, PendingAskUserSnapshot, PlanConstraints, PlanContextConfig,\n  PlanEnterSessionCallback, PlanExecutionEntry, PlanExitCallback, PlanNode, PlanState, ProcessSubprocessRequest,\n  ProjectionPolicy, ProvideWorkflowNode, ReasoningItem, ReasoningTextPart, RecallLayerOutput, RecallParams,\n  RecallResult, RefusalPart, RestoreContextOptions, RetryPolicy, ReturnParams, ReturnResult,\n  RunnableLoopHarness, RunnableLoopOpts, ScopedStorage, SdkStreamEvent, SequenceWorkflowNode, SerializedError,\n  ServerToolItem, ServerToolSpec, SessionScope, SessionSeedHarness, SettleResult, ShellAdapter,\n  ShellExecOptions, ShellExecResult, Snapshot, Span, SpawnParams, SpawnResult,\n  SpawnWorkflowNode, StallNudgeOpts, SteeringConfig, SteeringDecision, SteeringRule, SteeringState,\n  Step, StepBranch, StepFork, StepForkAll, StepForkRace, StepForkSettle,\n  StepLLM, StepLedgerRetention, StepLedgerStats, StepLoop, StepMeta, StepProvide,\n  StepRun, StepSpawn, StepSubHarness, StepSubprocessOverrides, StepSubprocessRequest, StepTool,\n  StepWorkflowOpts, StorageAdapter, StoreParams, StoreResult, StreamEvent, StreamingItem,\n  SubHarness, SubHarnessBuiltinTool, SubHarnessContinueState, SubHarnessContinueTurnOptions, SubHarnessFinishReason, SubHarnessPromptTurnOptions,\n  SubHarnessResumeState, SubHarnessRunContext, SubHarnessSession, SubHarnessSessionPolicy, SubHarnessSettings, SubHarnessStartOptions,\n  SubHarnessStreamPart, SubHarnessTurnResult, SubHarnessWorkflowNode, SubflowWorkflowNode, SubprocessAdapter, SubprocessControlResult,\n  SubprocessHandle, SubprocessHandleMetadata, SubprocessRequest, SubprocessStatus, SubprocessStopResult, SummaryTextPart,\n  TemporalContextConfig, TemporalFact, TemporalSearchResult, TokenUsage, Tool, ToolContext,\n  ToolContextDeclaration, ToolExecutionContext, ToolWorkflowNode, TraceExporter, Until, UntilPredicate,\n  Verdict, VerifyFn, WebSearchItem, WhenClause, WorkflowDocument, WorkflowNode,\n  WorkingMemoryContextConfig, WorkingMemoryContextState,\n} from '@noetic-tools/core';\nimport {\n  AgentHarness, AskUserAnnotationSchema, AskUserInputSchema, AskUserOptionSchema, AskUserOutputSchema, AskUserQuestionSchema,\n  CheckpointKeys, CheckpointSchemaVersion, CheckpointSnapshotSchema, CwdSnapshotSchema, DEFAULT_NUDGE_MESSAGE_TEXT, DEFAULT_STEP_LEDGER_RETENTION,\n  DetachedStatus, FrontierFrameSchema, GenAI, InMemoryExporter, ItemLogSnapshotSchema, ItemSchema,\n  ItemSchemaRegistry, LedgerEntryKind, MergeStrategySchema, NoeticAttr, NoeticConfigError, NoeticErrorImpl,\n  NoopExporter, PendingAskUserSnapshotSchema, PlanNodeSchema, PlanPhase, SUB_HARNESS_KINDS, Slot,\n  SteeringAction, SubHarnessKind, SubHarnessStreamPartSchema, TIMEOUT_ERROR_PREFIX, ToolAttr, UntilPredicateSchema,\n  WorkflowDocumentSchema, WorkflowNodeSchema, adaptivePlan, aiCondition, all, allCondition,\n  any, anyCondition, branch, channel, compilePlan, context,\n  cosineSimilarity, createCheckpointStore, createDetachedSignal, createInMemoryFsAdapter, createInMemoryShellAdapter, createInMemoryStorage,\n  createInMemorySubprocessAdapter, createMessage, createNudgeMessage, createOpenRouterEmbed, createStallNudgeHook, defaultItemSchemaRegistry,\n  durableTaskState, dynamicWorkflow, embeddingMatch, estimateTokens, every, execute,\n  fileReference, findFunctionCall, fork, getRegistry, getToolCwd, historyWindow,\n  hydrateNode, hydrateWorkflow, interview, isNoeticConfigError, isNoeticError, isServerToolSpec,\n  layerData, layerFn, lookupStep, loop, observationalContext, otherwise,\n  parseAndRunWorkflow, planContext, provide, ralphWiggum, react, registerStep,\n  runnableLoop, seedFromItems, semanticRoute, semanticSwitch, serializeError, setToolCwd,\n  snapshotCwdState, spawn, staticContent, steering, step, storageGetMany,\n  stripUnresolvedToolCalls, temporalContext, tool, toolContextLayer, toolWithGenerator, until,\n  validateWorkflow, walkWorkflow, when, workflowDepth, workingMemoryContext,\n} from '@noetic-tools/core';\n\ndeclare const searchTool: Tool;\ndeclare const calcTool: Tool;\ndeclare const agent: Step<ContextData, string, string>;\ndeclare const observer: (buffer: ReadonlyArray<unknown>) => Promise<string[]>;\ndeclare const semanticRecall: ContextLayer;\n";
