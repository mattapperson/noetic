/**
 * Ink JSX type declarations.
 *
 * Ink v5 ships with TypeScript types out of the box. This file provides
 * any necessary module augmentation for React JSX namespace compatibility.
 *
 * Ink components (Box, Text, etc.) are typed through the `ink` package itself.
 * Import them directly:
 *   import { Box, Text, render, useInput, useApp } from 'ink';
 *   import TextInput from 'ink-text-input';
 */

export {};
