/**
 * @noetic-tools/openui/server — the OpenUI transport: a web-standard
 * `serveOpenUi` fetch handler that speaks the OpenUI protocol over a Noetic
 * `AgentHarness`, plus the client-side descriptors for OpenUI's `fetchLLM`.
 */

export * from './client';
export * from './protocol';
export * from './serve';
