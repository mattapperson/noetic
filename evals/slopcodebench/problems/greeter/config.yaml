version: 1
name: greeter
description: >-
  Minimal, self-contained smoke problem for the Noetic SlopCodeBench harness.
  One checkpoint, one core test — cheap to run and deterministic, used to prove
  the agent → workspace → pytest grading pipeline end-to-end.
tags:
  - smoke
category: example
difficulty: Easy
entry_file: greeter
timeout: 10

checkpoints:
  checkpoint_1:
    version: 1
    order: 1
    state: Core Tests
    timeout: 30
