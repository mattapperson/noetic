diff --git a/esm/lib/model-result.js b/esm/lib/model-result.js
index ade685ae633da63b7220e8dc9be218621d6a328a..4308c9bed642c798b475af764d0562bc4b068bb0 100644
--- a/esm/lib/model-result.js
+++ b/esm/lib/model-result.js
@@ -470,13 +470,25 @@ export class ModelResult {
             : originalInput
                 ? [{ role: 'user', content: originalInput }]
                 : [];
-        const newInput = [
+        const rawInput = [
             ...normalizedOriginalInput,
             ...(Array.isArray(currentResponse.output)
                 ? currentResponse.output
                 : [currentResponse.output]),
             ...toolResults,
         ];
+        // Deduplicate function_call items by callId (keep last occurrence)
+        const seenCallIds = new Set();
+        const newInput = [];
+        for (let i = rawInput.length - 1; i >= 0; i--) {
+            const item = rawInput[i];
+            if (item?.type === 'function_call' && item.callId) {
+                if (seenCallIds.has(item.callId)) continue;
+                seenCallIds.add(item.callId);
+            }
+            newInput.push(item);
+        }
+        newInput.reverse();
         if (!this.resolvedRequest) {
             throw new Error('Request not initialized');
         }
